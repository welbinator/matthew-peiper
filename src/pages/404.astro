---
import Base from "../layouts/Base.astro";
import { withBase } from "../lib/base";
---

<Base title="Page not found">
	<div class="not-found">
		<div class="not-found-code">404</div>
		<h1>Page not found</h1>
		<p>The page you're looking for doesn't exist or has been moved.</p>
		<p class="redirect-notice">Taking you home in <span id="countdown">8</span> seconds…</p>
		<div class="not-found-actions">
			<a href={withBase("/")} class="btn btn-primary btn-lg">Go home now</a>
			<a href={withBase("/contact")} class="btn btn-secondary btn-lg">Contact us</a>
		</div>
	</div>
</Base>

<style>
	.not-found {
		text-align: center; padding: var(--spacing-5xl) var(--spacing-lg);
		max-width: var(--max-width); margin: 0 auto;
	}
	.not-found-code {
		font-size: 10rem; font-weight: var(--font-weight-display); line-height: 1;
		letter-spacing: -0.05em; color: var(--color-brand); opacity: 0.3;
		margin-bottom: var(--spacing-lg);
	}
	.not-found h1 { font-size: var(--font-size-3xl); margin-bottom: var(--spacing-md); }
	.not-found p { font-size: var(--font-size-lg); color: var(--color-muted); margin-bottom: var(--spacing-2xl); }
	.redirect-notice { font-size: var(--font-size-sm) !important; opacity: 0.7; margin-top: calc(var(--spacing-md) * -1); }
	.not-found-actions { display: flex; gap: var(--spacing-md); justify-content: center; flex-wrap: wrap; }
	@media (max-width: 480px) {
		.not-found-code { font-size: 6rem; }
		.not-found-actions { flex-direction: column; }
		.not-found-actions .btn { width: 100%; }
	}
</style>

<script>
	const el = document.getElementById("countdown");
	if (el) {
		let t = 8;
		const iv = setInterval(() => {
			t--;
			el.textContent = String(t);
			if (t <= 0) {
				clearInterval(iv);
				window.location.replace(`${document.querySelector('a.btn-primary')?.getAttribute('href') || '/'}`);
			}
		}, 1000);
	}
</script>
